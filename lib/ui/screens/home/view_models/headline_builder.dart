import 'package:flutter/material.dart';

class HeadlineBuilder extends StatelessWidget {
  const HeadlineBuilder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

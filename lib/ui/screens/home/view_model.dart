import 'package:flutter/cupertino.dart';

class NoInternetConnexion extends StatelessWidget {
  const NoInternetConnexion({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
